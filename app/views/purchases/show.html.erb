<%- model_class = @purchase.class -%>
<h1><%=t '.title', :default => model_class.model_name.human %> Confirmation</h1>
<div id="notice"><%= notice %></div>
<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:email) %>:</strong></dt>
  <dd><%= @purchase.email %></dd>
  <dt><strong>Cost</strong></dt>
  <dd>$<%= @purchase.amount %></dd>
  <dt><strong><%= model_class.human_attribute_name(:quantity) %>:</strong></dt>
  <dd><%= @purchase.quantity %> lbs</dd>
  <dt><strong>Date:</strong></dt>
  <dd><%= @purchase.created_at %></dd>
</dl>
<p>You can pickup your rocks <a href="http://www.google.com/mars/#lat=-1.713611&lon=-6.196289&zoom=8&q=spacecraft">here.</a></p>
<p>Recommended <a href="http://www.esa.int/esaMI/Aurora/SEM1PM808BE_0.html">shipping method.</a></p>
<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              purchases_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_purchase_path(@purchase), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              purchase_path(@purchase),
              :method => 'delete',
              :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
              :class => 'btn btn-danger' %>
</div>
